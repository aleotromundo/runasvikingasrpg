// Dirección visual: Amber War Room — la ruta raíz entra directamente al fiordo; no hay shell de plantilla que diluya la atmósfera.

import Home from "@/pages/Home";

export default function App() {
  return <Home />;
}
